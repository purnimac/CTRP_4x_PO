webdriver.class=${test.webdriver.class}
phantomjs.binary.path=${test.phantomjs.binary.path}

server.hostname=${jboss.hostname}
server.port=${jboss.port}
server.jndi.port=${jboss.naming.port}

jdbc.user-name=${jdbc.username}
jdbc.password=${jdbc.password}

jdbc.driver-class=${jdbc.driverClassName}
jdbc.connection-url=${jdbc.url}

# Used to interactive the with JBoss JMX Console, defaults to admin/admin
jboss.jmx.username=${jboss.jmx.username}
jboss.jmx.password=${jboss.jmx.password}

webservice.client.username=${webservice.client.username}
webservice.client.password=${webservice.client.password}